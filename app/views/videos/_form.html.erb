<%= form_for @v, html: { multipart: true } do |f| %>
  <% if @v.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% @v.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :video %>
    <%= f.file_field :video, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>